.dat ファイル

u,v,w,p が (k, i, j)で定義されているのがうっとうしいので(i, j, k)の順に直す。(それとも何か理由があるのか??)(←p78に説明がある)

1000 format 　みたいなやつ、これは別の書き方ができないのか? そのためにすべての行を6文字くらい空けて書いている

初期値を可視化せずそれより後の流れ場のみparaviewに読み込ませるのは厳密には適切ではない

出力ファイル名にdnsを付ける必要はないし、、消していいかも
例えば、Reを変えてプログラムを回した時に、そのReがフォルダ名及びファイル名に反映され、新規に出力フォルダが作成される(同じReだったら上書き)とかだったら便利かも

始めのコンソール上の出力で
poi err
のところがinfinityになってる

data 宣言で000~100 までやってるが、、あれ必要か?

終了時刻が分かりにくい。
CT=100 で、プログラム内で自力でファイル名を用意し、その数、つまり100個までしか出力できないようになっている
iskip=40 ごとに1回(コンソール上に?)出力することをisave=100 回行う。
dt = 0.0001 なので、終了時刻は dt * iskip * isave = 0.0001 * 40 * 100 = 0.4 (秒)
(実際に T=0.399で出力が終わる。)

iskipやisaveはglobal変数に含めることはできないか?

CT は 000 ~ 100 の101通りの文字列を入れておくためのただの箱
ICT はCTの何番目の要素かを指定するための値(0~100)
絶対に改善しないといけないこととしては、 自分でisave を100以上にしたいときに、出力ができないこと
_____________________________
本格的に書き換える前のプログラムは
fortran / kaji / main
に出力ファイルと共にある


\\\\\\\\\\\\\\\\\\\\\
覚書
文字変数(あとから文字を入れる、置き直す)の場合は
character(文字数):: 仮引数
と宣言する
