.dat ファイル

go to 文はwhile文に置き換える

u,v,w,p が (k, i, j)で定義されているのがうっとうしいので(i, j, k)の順に直す。(それとも何か理由があるのか??)

1000 format 　みたいなやつ、これは別の書き方ができないのか? そのためにすべての行を6文字くらい空けて書いている

初期値を可視化せずそれより後の流れ場のみparaviewに読み込ませるのは厳密には適切ではない

そもそもなぜ文字変数など使っている??
出力ファイル名にdnsを付ける必要はないし、、消していいかも


_____________________________
本格的に書き換える前のプログラムは

fortran / kaji / main
に出力ファイルと共にある


\\\\\\\\\\\\\\\\\\\\\
覚書
文字変数(あとから文字を入れる、置き直す)の場合は
character(文字数):: 仮引数
と宣言する
